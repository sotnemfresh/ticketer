services:
  postgres:
    image: postgres:18.6
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: ticketer
    ports:
      - "5432:5432"
    volumes:
      - postgres:/var/lib/postgresql
volumes:
  postgres: